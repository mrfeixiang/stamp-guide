# 部署到 GitHub Pages — 操作清单（多文件版）

翔，这是标准多文件结构的网站,以后扩展方便。整个部署约 5 分钟,不用写代码。

## 你拿到的结构

```
stamp-site/
├── index.html              # 页面骨架(空壳,内容由 JS 注入)
├── assets/
│   ├── css/styles.css      # 所有样式
│   └── js/
│       ├── content.js      # ← 所有三语内容都在这里(以后改这个)
│       └── i18n.js         # 渲染 + 语言切换逻辑
├── .nojekyll               # 空文件,告诉 Pages 别用 Jekyll(重要)
└── README.md
```

⚠️ **关键**: 上传时要保持这个**目录结构**(assets/css、assets/js 文件夹层级不能乱),否则 index.html 找不到 css/js,页面会变成没样式的纯文本。

---

## 方法 A — 命令行(多文件强烈推荐这个,最不容易传错结构)

```bash
# 进入 stamp-site 文件夹(里面能看到 index.html)
cd stamp-site

git init
git add .                      # 注意:加 . 会把 assets/ 整个目录和 .nojekyll 一起加进来
git commit -m "STAMP trilingual journal club guide"
git branch -M main
git remote add origin https://github.com/<你的用户名>/stamp-guide.git
git push -u origin main
```

> 先在 github.com 建一个空的 Public 仓库(名字如 `stamp-guide`,**不要**勾选 Add README,保持空),再跑上面命令。
> `git add .` 会自动包含隐藏文件 `.nojekyll` 和整个 `assets/` 目录,结构原样上传,不会乱。

然后:仓库 → **Settings → Pages → Source 选 Deploy from a branch → main / (root) → Save**,等 1 分钟。

---

## 方法 B — 网页拖拽(可行,但要小心目录结构)

GitHub 网页上传**支持拖整个文件夹**(会保留子目录):

1. github.com → New repository → 名字 `stamp-guide` → Public → Create(保持空仓库)
2. 仓库页 → **Add file → Upload files**
3. **把整个 `stamp-site` 文件夹里的内容拖进去**——直接把 `index.html`、`assets`(整个文件夹)、`.nojekyll`、`README.md` 一起选中拖入。Chrome/Edge 会保留 `assets/css/`、`assets/js/` 的层级。
   - ⚠️ `.nojekyll` 是隐藏文件,macOS 访达里按 `⌘ + ⇧ + .` 显示后再拖。
   - ⚠️ 如果拖文件夹后发现 css/js 没进去,改用方法 A(命令行最稳)。
4. Commit changes
5. Settings → Pages → Deploy from branch `main` `/root` → Save

---

## 上线后

网址: `https://<你的用户名>.github.io/stamp-guide/`
直接发这个链接给 lab 成员或同学。手机也能正常看,语言选择会记在浏览器里。

## 以后想加内容?

打开 `assets/js/content.js`:
- **改某段**: 在 `SECTIONS[]` 里找到对应区块,改 `title` 或 `html`。三语(`.lang-ko`/`.lang-zh`/`.lang-en`)记得一起改。
- **加一个新区块**: 往 `SECTIONS[]` 末尾加一个 `{ id, num, title, html }` 对象,再往 `TOC[]` 加一条目录。页面会自动重建,**不用碰 index.html**。
- 公式里反斜杠要写两个(JS 字符串转义),比如 `\\alpha`、`\\sum`。

改完 commit,网站 1 分钟内自动更新。

## 小提醒

- 本地直接双击 `index.html` 打开时,**有些浏览器会因安全策略不执行本地 JS**(页面空白)。这是本地预览的限制,**部署到 GitHub Pages 后完全正常**。想本地预览就在文件夹里跑 `python3 -m http.server` 再访问 `http://localhost:8000`。
- 仓库 Public 只公开了网页源码,无任何私密信息,安全。
